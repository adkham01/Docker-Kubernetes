apiVersion: v1
kind: Service
metadata: 
  name: load-balancer-service
  labels:
    app: front-end
spec: 
  type: LoadBalancer
  ports:
  - targetPort: 80
    port: 80 
    nodePort: 30008
  selector:
    app: front-end  
