apiVersion: v1
kind: Pod
metadata:
  name: java-app
spec:
  containers:
  - name: java-app-container
    image: adkham01/java-app
    ports:
    - containerPort: 8080
    envFrom :
    - configMapRef:
        name: app-config

