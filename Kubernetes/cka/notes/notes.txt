etcd
-----------------------------------------------
       V3                    V2               |
etcdctl snapshot save | etcdctl backup        |
etcdctl enpoint health| etcdctl cluster-healt |
etcdctl get           | etcdctl mk/mkdir      |
etcdctl put           | etcdctl set           |
-----------------------------------------------
export ETCDCTL_API=3 => To set Right version
kubeCtl exec etcd-controlplane -n kube-system -- sh -c "ETCDCTL_API=3 etcdctl get /
  --prefix -- keys-only --limit=10 / \
  --cacert /etc/kubernetes/pki/etcd/ca.crt \
  --cert /etc/kubernetes/pki/etcd/server.crt \
  --key /etc/kubernetes/pki/etcd/server.key

To take a snapshot from a running etcd server

For offline file-level backup of the data directory
etcdutl backup \ 
  --data-dir /var/lib/etcd \ 
  --backup-dir /backup/etcd-backup

Checking Snapshot Status  
etcdctl snapshot status /backup/etcd-snapshot.db \ --write-out=table

To restore a snapshot to a new data directory:
etcdutl snapshot restore /backup/etcd-snapshot.db \ --data-dir /var/lib/etcd-restored

k get pods --no-headers | wc -l

ConfigMaps
----------
k create cm app-config \
  --from-literal=APP_COLOR=blu \
  --fromloteral=APP_MOD=prod

k create cm app-config \
  --from-file=app_config.properties  

Secrets
-------
k create secret generic app-secret \
  --from-literal=DB_HOST=mysql \
  --from-literal=DB_USER=root \
  --from-literal=DB_PASSWORD= paswrd

k create secret generic app-secret \
  --from-file=app_secret.properties 

echo -n 'mysql' | base64    

AutoScaling
-----------
k autoscale deploy my-app \
  --cpu-percent=50 --min=1 --max=10

Cluster maintenance
-------------------  
k drain node-1 => Pods are gacefully terminated
k cordon node-1 => Pods will not be scheduled on the node
k uncordon node-1 => Node came back for scheduling
apt-cache madison kubeadm => displays all available versions of kubeadm
snap info kubeadm => same command for snap
apt-get install -y --alllow-change-hel-packages kubeadm=version
kubeadm upgrade plan => checks what k8s versions we can upgrade 
sudo kubeadm upgrade apply v1.20.0
apt-get install -y --allow-change-held-packages kublet = 1.20.x kubectl=1.20.xupgrade kublet and kubectl

Security
--------
k crt approve admin
k crt deny admin

Roles
k create role pod-reader --verb=get --verb=watch --resources=pods
k create clusterrole pod-reader --verb= get,list,watch --resources=pods

RoleBindings
k create rolebinding admin --clusterrole=pod-reader --user=jane --user=jan --group=dev
k create clusterrolebinding cluster-admin clusterrole=pod-reader --user=jan --group=dev

ServiceAccount
k create sa dashboard-sa